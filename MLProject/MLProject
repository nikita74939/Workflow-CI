name: telco-churn

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      C: {type: float, default: 1.0}
      solver: {type: string, default: "liblinear"}
    command: "python modelling.py {C} {solver}"